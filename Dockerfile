# FROM <name of container repo>

# COPY <file on host> <file copy to container>

# "RUN" runs command(s) to execute on container build
# RUN <cmd>
# More ...

# "CMD" runs an initial command on container startup
CMD bash
